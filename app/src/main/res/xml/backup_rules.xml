<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Include the DB and DataStore so data survives app updates and device restores.
         Android 9+ encrypts backups with the user's Google account key before upload. -->
    <include domain="database" path="family_home.db" />
    <include domain="file" path="datastore" />
</full-backup-content>
